<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:zxing="clr-namespace:ZXing.Net.Maui.Controls;assembly=ZXing.Net.Maui.Controls"
             x:Class="Andro.Views.ScanPage"
             Shell.TabBarIsVisible="False"
             Title="Scan QR code"
             BackgroundColor="Black">
    <Grid RowDefinitions="*,Auto">
        <zxing:CameraBarcodeReaderView x:Name="Reader" Grid.RowSpan="2" IsDetecting="False" BarcodesDetected="OnBarcodesDetected" />

        <!-- Viewfinder -->
        <Border WidthRequest="250" HeightRequest="250" StrokeThickness="3" Stroke="White" StrokeShape="RoundRectangle 18"
                BackgroundColor="Transparent" HorizontalOptions="Center" VerticalOptions="Center" InputTransparent="True" />

        <VerticalStackLayout Grid.Row="1" Padding="24,16,24,32" Spacing="12" BackgroundColor="#99000000">
            <Label x:Name="Hint" TextColor="White" HorizontalTextAlignment="Center" FontSize="15"
                   Text="Point the camera at your contact's QR code (My ID in their app)." />
            <Button x:Name="TorchButton" Text="Turn on light" Clicked="OnTorchClicked" HorizontalOptions="Center"
                    BackgroundColor="Transparent" BorderColor="White" BorderWidth="1" TextColor="White" />
        </VerticalStackLayout>
    </Grid>
</ContentPage>
