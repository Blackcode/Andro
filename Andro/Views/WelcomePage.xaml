<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:Andro.ViewModels"
             x:Class="Andro.Views.WelcomePage"
             x:DataType="vm:WelcomeViewModel"
             Shell.NavBarIsVisible="False">
    <ScrollView>
        <VerticalStackLayout Padding="28,48" Spacing="18" MaximumWidthRequest="520">
            <Label Text="Andro" Style="{StaticResource Headline}" />
            <Label Text="Private messages that no single company or government can switch off." Style="{StaticResource SubHeadline}" />

            <Label Style="{StaticResource Caption}" FontSize="14"
                   Text="• No phone number, email or account. Your identity is a key created on this phone.&#10;• Messages are end-to-end encrypted and hide who is talking to whom.&#10;• They travel through many independent relays around the world, optionally over Tor." />

            <Button Text="Create my private identity" Command="{Binding CreateCommand}" IsEnabled="{Binding IsBusy, Converter={StaticResource InvertedBool}}" />

            <Label Text="Already have an identity?" Style="{StaticResource SectionTitle}" />
            <Entry Placeholder="Paste your backup key (nsec1…)" Text="{Binding BackupKey}" IsPassword="True" IsSpellCheckEnabled="False" IsTextPredictionEnabled="False" />
            <Button Text="Restore" Style="{StaticResource SecondaryButton}" Command="{Binding RestoreCommand}" IsEnabled="{Binding IsBusy, Converter={StaticResource InvertedBool}}" />

            <ActivityIndicator IsRunning="{Binding IsBusy}" IsVisible="{Binding IsBusy}" />
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
